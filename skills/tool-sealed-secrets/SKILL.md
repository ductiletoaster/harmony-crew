---
name: tool-sealed-secrets
description: Operating Sealed Secrets as an agent — sealing secrets into in-repo SealedSecret artefacts the controller decrypts in-cluster. Active only when the project recipe declares a tools.* entry whose module = sealed-secrets.
---

Stub for the V1 generality test — TODO real module.
