# Minimal VALID recipe — must PASS recipe-lint.
# Only an identity block and a single argocd tool entry (tool-argocd is the one
# tool skill that exists in the foundation today).

identity:
  name: minproj
  repo: example/minproj

tools:
  gitops:
    module: argocd
    endpoint: x
    auth: y
    mcp: argocd
